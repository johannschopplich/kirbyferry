/** Default directory for extracted JSON, relative to the working directory. */
export const DEFAULT_OUT_DIR: string = 'content-fields'

/** Conventional Kirby content root locations, probed in order. */
export const CONTENT_ROOT_CANDIDATES: string[] = ['content', 'storage/content']
